name: book_tracker
description: A book learning progress tracker.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  hive_flutter: ^1.1.0
  path_provider: ^2.1.5
  file_picker: ^8.1.7
  uuid: ^4.5.1
  table_calendar: ^3.1.2
  intl: ^0.19.0
  permission_handler: ^11.3.1
  archive: ^3.6.1   # <-- добавлено

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.13
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true